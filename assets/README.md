# Relay project assets

- `videos/demos/` — product and feature demo videos.
- `images/architecture/` — project architecture visuals.
- `previews/` — standalone HTML previews and design artifacts.

Runtime uploads remain in `server/uploads/`, and frontend application assets remain in `client/public/`.
