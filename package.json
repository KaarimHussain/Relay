{
  "name": "relay",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"client,server\" --prefix-colors \"cyan,yellow\" \"npm run dev --prefix client\" \"npm run start:dev --prefix server\"",
    "build": "npm run build --prefix client && npm run build --prefix server",
    "install:all": "npm install --prefix client && npm install --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
